# Telegram bot token
BOT_TOKEN=000000:REPLACE_ME

# Where images are served from
PUBLIC_BASE=https://cdn.mangalair.ru

# Where your front lives (Cloudflare Pages)
FRONTEND_URL=https://app.mangalair.ru

# Uvicorn host/port
HOST=0.0.0.0
PORT=8000

# SQLite path (absolute recommended in prod). For Timeweb guide use /opt/mangalair/data/data.db
DATABASE_URL=sqlite:////opt/mangalair/data/data.db?check_same_thread=false